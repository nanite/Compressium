name: Build & Snapshot

on:
  push:
    branches:
      - "dev/**"

jobs:
  build:
    if: |
      !contains(github.event.head_commit.message, '[ciskip]')
    uses: nanite/workflows/.github/workflows/base-java.yml@1.1
    secrets:
      nanite-token: ${{ secrets.NANITE_DEPLOY }}
    with:
      java-version: 25
      use-snapshots: true
      gradle-tasks: build publish
      release-to-github: false
